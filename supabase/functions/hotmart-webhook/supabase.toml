verify_jwt = false

# CORS é controlado no código via getSecureHeaders() - não usar allow_origins = ["*"]
[cors]
allow_headers = ["authorization", "x-client-info", "apikey", "content-type", "x-cakto-signature", "x-cakto-token"]

