/**
 * Centraliza classes de aspect-ratio para manter consistência visual.
 *
 * Importante: manter os valores como strings literais dentro de `src/`
 * para o Tailwind conseguir detectar e gerar as classes.
 */

export const ASPECT_4_3 = "aspect-[4/3]" as const;





















