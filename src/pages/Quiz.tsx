import QuizCheckoutFlow from "@/components/QuizCheckoutFlow";

const Quiz = () => {
  return (
    <div className="min-h-screen bg-cream-100 flex items-center justify-center px-4 py-10">
      <QuizCheckoutFlow mode="page" />
    </div>
  );
};

export default Quiz;
